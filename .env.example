# Copy to .env and fill in your own values.
# app/config.py reads these; .env is already git-ignored.

SECRET_KEY=change-me-to-a-long-random-string

DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=OnlineExaminationDB
